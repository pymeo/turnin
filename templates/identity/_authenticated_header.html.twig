<header class="session-header" aria-label="Sesión">
    <a href="{{ path('identity_app') }}" class="session-brand"><span class="session-mark" aria-hidden="true">T</span><span>Turnin</span></a>
    <form method="post" action="{{ path('identity_logout') }}">
        <input type="hidden" name="_csrf_token" value="{{ csrf_token('logout') }}">
        <button class="btn-ghost" type="submit">Salir</button>
    </form>
</header>
