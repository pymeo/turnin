[www]
; The development container already runs as an unprivileged user, so FPM must
; not try to drop privileges again (it would only emit a warning and ignore it).
listen = 9000
clear_env = no

; Surface worker output in `docker compose logs` instead of swallowing it.
catch_workers_output = yes
decorate_workers_output = no
