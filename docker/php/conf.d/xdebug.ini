; Xdebug is present in the development image but idle unless XDEBUG_MODE says
; otherwise, so `make test` and `make qa` keep full speed.

xdebug.mode = ${XDEBUG_MODE}
xdebug.client_host = host.docker.internal
xdebug.client_port = 9003
xdebug.start_with_request = trigger
xdebug.idekey = turnin
