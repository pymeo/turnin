security:
    # https://symfony.com/doc/current/security.html#registering-the-user-hashing-passwords
    password_hashers:
        App\Platform\Identity\Infrastructure\Security\SecurityUser: 'auto'

    # https://symfony.com/doc/current/security.html#loading-the-user-the-user-provider
    providers:
        database_users:
            id: App\Platform\Identity\Infrastructure\Security\DatabaseUserProvider

    firewalls:
        dev:
            # Ensure dev tools and static assets are always allowed
            pattern: ^/(_profiler|_wdt|assets|build)/
            security: false
        main:
            lazy: true
            provider: database_users
            form_login:
                login_path: identity_login
                check_path: identity_login
                enable_csrf: true
                default_target_path: /app
                success_handler: App\Platform\Identity\Infrastructure\Security\PostAuthenticationSuccessHandler
            custom_authenticators:
                - App\Platform\Identity\Infrastructure\Security\GoogleAuthenticator
            logout:
                path: identity_logout
                target: web_home
                enable_csrf: true

            # Activate different ways to authenticate:
            # https://symfony.com/doc/current/security.html#the-firewall

            # https://symfony.com/doc/current/security/impersonating_user.html
            # switch_user: true

    # Note: Only the *first* matching rule is applied
    access_control:
        - { path: ^/api, roles: IS_AUTHENTICATED_FULLY }
        - { path: ^/app, roles: IS_AUTHENTICATED_FULLY }
        - { path: ^/onboarding, roles: IS_AUTHENTICATED_FULLY }
        - { path: ^/supervisor, roles: IS_AUTHENTICATED_FULLY }

when@test:
    security:
        password_hashers:
            # Password hashers are resource-intensive by design to ensure security.
            # In tests, it's safe to reduce their cost to improve performance.
            Symfony\Component\Security\Core\User\PasswordAuthenticatedUserInterface:
                algorithm: auto
                cost: 4 # Lowest possible value for bcrypt
                time_cost: 3 # Lowest possible value for argon
                memory_cost: 10 # Lowest possible value for argon
